#My Discrete Math Repository

#Homework 2 - 202628102 KANG MIN JAE

# Dmath

### 2.1 Write Mathematical Expressions

1. IF (P AND Q) THEN R
   $$
   (P \land Q) \rightarrow R
   $$

3. (P XOR Q) OR R
   $$ (P \oplus Q) \lor R $$

4. NOT P IFF Q
   $$ \neg P \leftrightarrow Q $$

5. FOR ALL x, P(x)
   $$ (\forall x), P(x) $$

6. THERE EXISTS AN x, NOT Q(x)
   $$ (\exists x), \neg Q(x) $$

7. IF P THEN Q EQUIVALENT TO NOT P OR Q
   $$ P \rightarrow Q \equiv \neg P \lor Q $$

8. Euler's Identity
   $$ e^{i\pi} + 1 = 0 $$

9. Sum of integers from 1 to 100
   $$ \sum_{n=1}^{100} n = 5050 $$

---

### 2.2 Translate Sentences into Logic Formulas

* ( p ): I study
* ( q ): I will pass the course
* ( r ): The professor accepts bribes

---

### Logical Translations

1. If I do not study, then I will only pass the course if the professor accepts bribes.
   $$ \neg p \rightarrow (r \rightarrow q) $$

2. If the professor accepts bribes, then I do not study.
   $$ r \rightarrow \neg p $$

3. The professor does not accept bribes, but I study and will pass the course.
   $$ \neg r \land p \land q $$

4. If I study, the professor will accept bribes and I will pass the course.
   $$ p \rightarrow (r \land q) $$

5. I will not pass the course but the professor accepts bribes.
   $$ \neg q \land r $$

---


# Dmath

## 📘 Mathematical Expressions

### 1. Logical Expressions

1. IF (P AND Q) THEN R

$$
(P \land Q) \rightarrow R
$$

2. (P XOR Q) OR R

$$
(P \oplus Q) \lor R
$$

3. NOT P IFF Q

$$
\neg P \leftrightarrow Q
$$

4. FOR ALL x, P(x)

$$
(\forall x), P(x)
$$

5. THERE EXISTS AN x, NOT Q(x)

$$
(\exists x), \neg Q(x)
$$

6. IF P THEN Q EQUIVALENT TO NOT P OR Q

$$
P \rightarrow Q \equiv \neg P \lor Q
$$

---

### 2. Famous Mathematical Formulas

7. Euler's Identity

$$
e^{i\pi} + 1 = 0
$$

8. Sum of integers from 1 to 100

$$
\sum_{n=1}^{100} n = 5050
$$

---

## 🧠 Translating Sentences into Logic Formulas

### Given propositions

* ( p ): I study
* ( q ): I will pass the course
* ( r ): The professor accepts bribes

---

### Logical Translations

1. If I do not study, then I will only pass the course if the professor accepts bribes.

$$
\neg p \rightarrow (r \rightarrow q)
$$

2. If the professor accepts bribes, then I do not study.

$$
r \rightarrow \neg p
$$

3. The professor does not accept bribes, but I study and will pass the course.

$$
\neg r \land p \land q
$$

4. If I study, the professor will accept bribes and I will pass the course.

$$
p \rightarrow (r \land q)
$$

5. I will not pass the course but the professor accepts bribes.

$$
\neg q \land r
$$

---

## 🔗 Repository Information

This repository is created for practicing mathematical expressions and logical formulas using Markdown and LaTeX.
